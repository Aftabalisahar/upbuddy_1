import 'package:flutter/material.dart';
class KeywordJobs extends StatelessWidget {
  const KeywordJobs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
