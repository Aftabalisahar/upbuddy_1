import 'package:flutter/material.dart';
class CatagoryJobs extends StatelessWidget {
  const CatagoryJobs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
